providers = []

[phases.setup]
nixPkgs = ["...", "python3", "pkg-config"]
nixLibs = ["stdenv.cc.cc.lib"]

[phases.install]
dependsOn = ["setup"]

[phases.build]
dependsOn = ["install"]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"
